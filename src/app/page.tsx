export default function Home() {
  /* const usersResponse = await fetch(`${process.env.API_URL}/users`, {
    headers: {
      Cookie: cookies().get(AUTH_COOKIE)?.value.toString() as string,       forma de hacer llamado con las cookies
    },
  });

  const users = await usersResponse.json();
  console.log(users); */

  return <></>;
}
